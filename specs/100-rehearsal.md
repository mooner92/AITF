# 100 — P9: 리허설·완료 판정 (생략 금지)

**상태**: Draft · 차단: 전체

## 목표

개강 전에 학생의 전체 여정과 장애 시나리오를 실측으로 통과시킨다. "될 것이다"는 완료가 아니다.

## 리허설 체크리스트

- [ ] **전 과정 완주** (테스트 계정): 학원 IP → 브라우저 터미널 → 프롬프트 → 1분 내 그래프 반영 → `add/commit/push`
- [x] **동시 부하 테스트 (1차, 2026-08-16 합성 부하)** — 통과
  - 조건: 학생 계정 6개가 동시에 각 ~586MB 상주 + CPU 연속 점유 (45초)
  - 결과: 서버 메모리 5.4G → **8.8G**(여유 13G 유지), load 1.94(4코어 기준 여유), swap 사용 0
  - **기존 서비스 전원 무영향**: 컨테이너·pm2 3종·gitea·cloudflared·nginx 전부 정상, 내부 응답 **2ms대** 유지
  - 정리 스크립트 실행 후 계정별 점유 전부 0MB로 회수 확인
  - ⚠️ 합성 부하라서 실제 AI CLI의 I/O·네트워크 대기 패턴은 미반영 → **키 발급 후 실 CLI로 2차 재시험 필요**
- [ ] hard cap 실동작 소액 테스트
- [x] 스냅샷 → 복구 왕복 1회 ([080](080-snapshot.md)에서 완료)
- [ ] **스킬 사용 시 토큰 증감 실측** — "왕복이 줄어든다" 멘트의 근거 확보 (그래프가 반대로 나오면 신뢰를 잃는다)
- [x] 반 사이 정리 스크립트 동작 확인 (부하 테스트 직후 메모리 100% 회수)
- [ ] **학생 관점 침투 셀프 체크** (전부 거부/차단이어야 통과):
  `sudo` / 타 학생 홈 열람 / `accounts.csv` 열람 / 하네스 수정 / 모니터링 웹 무인증 접근 / 타 학생 저장소 push

## 완료 판정 (개강 전 최종 10)

1. [ ] 부트 볼륨 백업 존재 + 인스턴스 리스크 해소 확인
2. [ ] 디스크 확장 완료 (잔여 150GB+)
3. [ ] 터널 3중 검증 후 22 폐쇄 (잠금 순서 지킴, 자택 예외 유지)
4. [ ] hard cap 실동작 확인
5. [ ] 전역 gitignore가 키·하네스를 실제로 차단
6. [ ] 하네스 읽기 전용 + 학생 저장소 미유출
7. [ ] tokscale 계정별 분리 집계
8. [ ] 스냅샷 → 복구 왕복 성공
9. [ ] 동시 부하 테스트 통과 + 기존 서비스 무영향
10. [ ] 침투 셀프 체크 전 항목 거부

## 수업일 운영 루틴 (리허설 때 함께 연습)

⚠️ **중등(14–16)→고등(16–18)은 쉬는 텀이 0분**이다. 교대 루틴을 그에 맞게 쪼갠다:

- **13:30 수업 전**: `free -h` / `df -h` / 기존 서비스 헬스 / 모니터링 웹 / 오늘 "밀리면 자르는 블록" 확인
- **16:00 교대 (1~2분 안에)**: `cleanup-class.sh mid` 한 줄만 — 메모리 즉시 회수 (스냅샷은 미루기). 고등반 시작하며 모니터링 웹 `free` 게이지 확인
- **18:00 수업 후**: `snapshot-class.sh mid <주차>` → `snapshot-class.sh high <주차>` → `cleanup-class.sh high` → summary.md 2개 훑기 → 오픈채팅 확인 → 다음 주 준비 메모
  (스냅샷을 수업 후로 모는 이유: 중등 스냅샷을 16시에 돌리면 고등반 수업과 I/O가 겹친다)
