# 060 — P5: Gitea + 학생 git 흐름

**상태**: Draft · 차단: 040

## 목표

학생 1인 1저장소. 학생 입장에서 git은 `add → commit → push` 3줄이 전부이도록 사전 구성(신원·인증·remote 전부 미리 주입).

## 설계

- **Gitea arm64 네이티브 바이너리** + 전용 시스템 계정 + systemd 유닛 (Docker 아님 — SELinux 볼륨 마찰 회피, RAM 절약)
- `HTTP_ADDR = 127.0.0.1`, 포트 3000 (외부 노출은 터널 `git.<domain>` 경유만)
- 공개 가입 **비활성화**. 관리자 비밀번호는 서버 비공개 노트에만.
- 학생은 로컬 계정이라 루프백으로 Gitea 웹 UI 접근 가능 — 전제하고 권한 설계 (본인 저장소만 쓰기).
- 계정·저장소는 CSV 기준 일괄 생성 + 반별 `class-wiki` 2개.
- **credential store 사전 주입**: 각 학생 홈 `.git-credentials`에 개인 토큰 — push 때 비밀번호 안 물음 (SSH 키 배포보다 마찰 0).
- 각 학생 프로젝트 폴더 `git init` + remote 등록까지 완료 상태로 제공.
- **전역 gitignore** (`--system` 한 번):
  ```bash
  printf ".env\n*.key\n.codex/\n.gemini/\nnode_modules/\nharness\n.harness/\n" | sudo tee /etc/gitignore
  sudo git config --system core.excludesfile /etc/gitignore
  ```

## 작업 체크리스트

- [ ] Gitea 설치 + systemd + 루프백 바인딩 + 가입 비활성화
- [ ] CSV 기준 계정·저장소·위키 생성 (API 스크립트)
- [ ] 학생별 git 신원 + credential + `git init`/remote 사전 주입
- [ ] 전역 gitignore 배치

## 검증 기준

- [ ] 학생 계정에서 `git add . && git commit && git push` → 비밀번호 질문 없이 성공
- [ ] `.env` 테스트 파일이 `git status`에 안 뜸
- [ ] 하네스 심링크가 push에 미포함
- [ ] 학생 A가 학생 B 저장소에 push 시도 → 거부

## 학생 모노레포 + 규칙 배포 (2026-08-25 확정)

**학생 저장소는 1인 1개로 고정한다(모노레포).** 매주 새 저장소를 만들지 않고
폴더가 늘어난다 — 12주 결과물이 한 저장소에 쌓여야 수료 시 GitHub 이전이
`git push --mirror` 한 번으로 끝나고, 커밋 이력도 한 줄기로 남는다.

```
project/
  README.md          저장소 안내 (materials/student-repo/README.md)
  MY-SERVER.md       포트·주소 (alloc-ports.sh 개인화 생성)
  rules/             강사가 배포하는 규칙 문서
  profile_card/ …    주차별 프로젝트 폴더 — rules/naming.md 의 이름 규칙
  public/            작품관 심링크
```

### 규칙 배포 — `push-rule.sh`

규칙 문서(naming·commit·sdd·make_sdd)는 `materials/rules/`에서 저작하고,
`scripts/push-rule.sh <이름>` 한 번으로 전 학생 저장소의 `rules/`에 들어간다.
수업 중 "지금 규칙을 떨어뜨린다" 자체가 장면이 된다 — 학생이 `ls rules/`로
발견하고, 열어 보고, 다음 커밋에 포함한다.

**심링크가 아니라 복사인 이유** (SERVER-RULES.md 방식과 다름):
배포 즉시 작업 트리 실파일로 나타나 Codex 가 바로 읽고, 학생의 다음
`git add .`에 자연히 포함돼 자기 히스토리가 되며, GitHub 이전 때 문서가
함께 간다. 검토한 대안 — root 직접 커밋(작업 트리 오염·author 꼬임),
Gitea push 후 학생 pull(전원 대기·머지 충돌), 심링크(이전 시 깨짐) — 모두 기각.

배포 시점은 커리큘럼 연동: naming=1주, commit=4주(git 배우는 주),
sdd·make_sdd=10주(SDD 주). `--sync` 는 신규·리셋 계정 복구용이며
`reset-test-account.sh`가 자동 호출한다.

## GitHub 이전 (수료 시 — 포트폴리오)

**질문**: Gitea로 관리하다 나중에 GitHub로 옮기면 커밋 이력·브랜치가 남는가?

**답: 커밋 이력은 완전히 남고, 설정은 남지 않는다.**

| 항목 | 이전되는가 | 방법·비고 |
|---|---|---|
| 커밋 이력 (author·날짜·메시지) | **예, 전부** | `git push --mirror` 한 줄. git 데이터는 플랫폼 무관 |
| 브랜치·태그 | **예, 전부** | mirror push에 포함 |
| branch protection rule | 아니오 | 플랫폼 설정이라 GitHub에서 다시 만든다 (수 분) |
| 이슈·PR | 아니오 | 우리 수업은 이슈·PR을 안 쓰므로 무관 |

**잔디(contribution graph)까지 살리려면 — 지금 해둬야 하는 것 하나**:
GitHub는 커밋의 author 이메일이 계정에 등록된 이메일과 일치할 때만 잔디에 찍는다.
계정 방침이 "학생 gmail 1개"로 확정됐으므로(120), **계정 생성 시 `git config user.email`을
학생 gmail로** 설정한다. 그러면 수료 후 학생이 그 gmail로 GitHub 계정을 만들어
mirror push 하는 순간 **12주치 잔디가 소급해서** 찍힌다. 고등부 포트폴리오 가치의 핵심.

수료 절차(12주차 "집에서 이어가는 법"에 포함): GitHub 계정 생성(gmail) → 새 저장소 →
`git push --mirror` → 프로필에 12주 이력·잔디 완성.

### 결정 기록 (추가)

- 2026-08-19 · 학생 git 신원의 email을 서버 로컬 주소가 아니라 **학생 gmail로 설정** —
  GitHub 이전 시 커밋 소유권·잔디 소급의 전제. 계정 생성 스크립트에 반영 필요.
- 2026-08-25 · **계정명 = 학생 gmail 앞부분** (강사 결정). 숫자로 시작하는 주소는
  도구 호환 문제가 있어 해당 학생만 숫자 접두어를 뗀 형태로 조정. 반·포트 번호는 계정명이
  아니라 roster.csv 에서 읽는다 (alloc-ports·deploy-skills·hub-status 수정).
- 2026-08-25 · **고등부 5명 계정·저장소·조직 생성 완료.** Gitea 조직 3개(mid/high/archive,
  private) + 반별 위키 저장소, 학생은 반 조직의 students 팀(write, code·wiki 한정).
  개인 project 저장소 + 토큰 + git 신원(실제 gmail) 배선, 실 push 로 검증
  — 커밋 author 가 gmail 로 찍히는 것 확인 (잔디 소급 전제 충족).
- 2026-08-25 · **모노레포 + push-rule 배포 체계 채택** (위 절). 복사 방식 채택 근거 포함.
- 2026-08-25 · **Gitea 는 상시 개방** (강사 결정 "그냥 열어놓자"). 계정·비밀번호가 있어야
  들어오고 남의 저장소는 안 보이므로 막을 이유가 없다. 정식 소개는 4주차(git 3동작 배우는
  주)에 "방금 push 한 게 여기 쌓인다" 장면으로, 2주차 위키 때 주소만 자연 노출.
