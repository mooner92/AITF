# 010 — P0: 인스턴스 보전·디스크 확장

**상태**: In Progress · 2026-08-16

## 목표

이후 모든 작업의 안전 기반 확보: ① 전체 복구 지점(부트 볼륨 백업) ② 인스턴스 유지 리스크 해소 ③ 학생 홈·Gitea·스냅샷을 담을 디스크 공간.

## 현재 상태 (실사)

- VM.Standard.A1.Flex **4 OCPU / 24GB** — Always Free 최대치. 셰이프 변경 불필요.
- 루트 파일시스템 29.5G 중 **96% 사용(잔여 1.4G)** — 이대로는 P3 이후 진행 불가.
- 단, 부트 볼륨은 200G인데 파티션이 44.5G만 사용 → **~155G 미할당**. 콘솔 작업 없이 서버 내 확장 가능.
- 즉효 정리 대상: npm 캐시 ~8.3G, docker 빌드 캐시 ~0.8G.

## ⚠️ 8/18 마감 — 사실 확인 완료 (2026-08-16)

Oracle이 2026-06-15부로 Always Free A1 한도를 **4 OCPU/24GB → 2 OCPU/12GB로 반감**했고(공지 없이),
**2026-08-18부터 초과 인스턴스를 자동 종료** 집행한다. 현 인스턴스(4/24)는 초과 상태다. 이틀 내 결정 필요:

- **옵션 A — 2 OCPU/12GB로 축소** (콘솔 Edit, 재부팅 발생, 데이터·IP 유지): 무료 확정. 수업은 반당 ~6명 순차 진행이라 12GB 운영 설계(계정당 1GB 상한 + 템플릿 홈 + swap 5G)로 감당 가능. 기존 서비스 포함 메모리 예산은 [040](040-accounts.md).
- **옵션 B — PAYG 전환 후 4/24 유지**: 지원 상담원발 "PAYG는 4/24 무료 유지" 보고 다수이나 **공식 문서 미확인** — 전환 전 문서/지원으로 재확인 필요. 결제수단 등록 리스크(실수 과금) 있음.

권장: **A(축소)** — 무료 확정 + 원래 세팅 가이드가 12GB 전제로 설계돼 있음. 축소 전 반드시 백업.

## 작업 체크리스트

- [ ] **부트 볼륨 백업** (운영자, OCI 콘솔 → Boot Volume → Create Backup) — **이 스펙의 게이트. 백업 전 서버 변경 금지**
- [ ] **8/18 전 A/B 결정 + 실행** (운영자): A면 콘솔 → 인스턴스 → More Actions → Edit → 2 OCPU/12GB (재부팅 후 기존 서비스 헬스 확인)
- [ ] ❌ 인스턴스 삭제 후 재생성 금지 — ap-seoul-1 A1은 "Out of capacity" 빈발, 재확보 실패 위험
- [ ] 디스크 확장 (백업 후):
  ```bash
  sudo /usr/libexec/oci-growfs -y
  # 실패 시 수동: growpart /dev/sda 3 → pvresize /dev/sda3
  #   → lvextend -l +100%FREE /dev/ocivolume/root → xfs_growfs /
  ```
- [ ] 캐시 정리: `npm cache clean --force` / `sudo docker builder prune -f`

## 검증 기준

- [ ] OCI 콘솔에 백업 항목 존재
- [ ] 8/18 이전에 A 또는 B 실행 완료 (`free -g`/`nproc`로 확인)
- [ ] `df -h /` 잔여 **150GB 이상** (디스크 확장은 셰이프와 무관 — 부트 볼륨 200GB는 프리티어 스토리지 한도 내)
- [ ] 기존 운영 서비스 전부 정상

## 결정 기록

- 2026-08-16(오전) · "축소 불필요, 4/24가 무료 최대"로 판단 — **오판이었음**.
- 2026-08-16(오후) · 웹 검색으로 사실 확인: 6/15부 무료 한도 2/12로 반감, 8/18부터 초과분 자동 종료. **축소(A)를 권장안으로 복원.** 교훈: 메타데이터는 "현재 스펙"만 알려주지 "정책 변화"는 알려주지 않는다.
- 2026-08-16 · 디스크는 블록 볼륨 추가가 아닌 부트 볼륨 미할당 영역 확장으로 해결 (비용 0, 무중단).

## 비공개 참조

클라우드 계정·공지 메일 원문·백업 명명은 서버 내 `~/class-setup/` 노트에 기록.
