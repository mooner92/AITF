# 자격증명·신원정보 — 어떤 경우에도 커밋 금지
.env
.env.*
*.key
*.pem
*credentials*
*.csv
accounts*
roster*
secrets*

# AI CLI 세션/설정 (키·로그 포함 가능)
.codex/
.gemini/
.claude/
.claude.json

# 로컬 작업물
node_modules/
*.log
.DS_Store
private/

# OMC 플러그인 세션 상태 (대화 로그 포함 가능 — 공개 금지)
.omc/

# Paperlogy TTF 원본 — 배포 zip에서 직접 풀어 쓴다 (약 6MB)
curriculum/fonts/

# 발표자료 — 원고와 엔진만 커밋하고 산출물은 빌드한다
decks/class-config.json
decks/*.html

# 랜딩 빌드 산출물 (폰트 임베드로 커짐 — 배포본은 서버에만)
web/landing/dist/
openai-keys.csv
decks/pptx/node_modules/
decks/pptx/package-lock.json
